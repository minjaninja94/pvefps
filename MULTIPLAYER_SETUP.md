# IRON SECTOR 멀티플레이 적용

GitHub의 최신 `cloud-ranking.js` / `firebase-config.js`를 기준으로 확장했습니다. 기존 `rankings` 컬렉션의 기록과 점수 계산, 싱글 랭킹 API를 유지합니다. 멀티 결과는 싱글 랭킹에 등록하지 않습니다.

## 관리자 설정: 두 가지만 적용

게임 코드는 GitHub Pages에서 그대로 실행됩니다. 별도의 Node 서버, Cloud Functions, Realtime Database 생성, 새로운 API 키는 필요하지 않습니다.

1. [Firebase Authentication](https://console.firebase.google.com/project/pvefps/authentication/providers) → 로그인 방법 → **익명(Anonymous)** → 사용 설정 → 저장. 플레이어가 직접 로그인할 필요 없이 브라우저가 임시 ID를 발급받습니다. 설정 전에도 기존 싱글 랭킹은 로그인 없이 계속 사용합니다.
2. [Cloud Firestore 규칙](https://console.firebase.google.com/project/pvefps/firestore/rules)에서 이 저장소의 **`firestore.rules` 전체 내용**을 붙여넣고 게시합니다. `rankings`는 기존처럼 읽기·신규 기록 저장이 가능하고 기존 기록 수정·삭제는 차단합니다. `mpRooms` 및 각 방의 `signals`만 멀티용으로 추가합니다. 다른 기존 컬렉션을 사용하도록 콘솔 규칙을 별도로 추가해 둔 경우에는 해당 블록도 보존해서 병합해야 합니다. 저장소에 남아 있던 과거의 전체 차단 규칙 파일은 현재 직접 저장하는 랭킹 JS와 맞지 않습니다.

Firebase 관리자 연결이 이 작업 환경에 제공되지 않아, 위 콘솔 설정은 코드 커밋으로 자동 반영되지 않습니다. 실제 콘솔 규칙 및 현재 요금제도 여기서 확인하지 못했습니다. SDK의 일반 공개 API 키로 관리자 설정을 변경할 수는 없습니다.

**무료만 사용하려면 프로젝트가 Spark인지 확인하고 결제 계정을 연결하지 마세요.** 새 유료 서비스·TURN 중계·Cloud Functions는 이 구현에 포함하지 않았습니다. Firebase 전체 배포 명령은 필요하지 않습니다. 특히 저장소에 남아 있는 구형 Functions 설정은 배포하지 마세요.

## 플레이

- 협력 작전: 이름 입력 → 난이도 선택 → 매칭 시작. 같은 난이도 대기실을 찾아 합류합니다. 최대 4명, 2명부터 준비할 수 있습니다. 시작 인원만큼 적·보스·파괴 파츠·방패 체력을 배로 늘립니다. 총 10스테이지, 5/10스테이지 보스. 사망자는 관전하고 다음 구역에서 부활하며, 전원 사망하면 종료합니다. 도중 이탈해도 해당 경기의 체력 배수는 유지합니다.
- PvP: 방 만들기 또는 방 코드/목록으로 입장 → 팀 1/2 선택. 최대 8명·팀당 4명·양 팀 최소 1명. 팀 합산 30킬 선취. 아군 피해 없음, 3초 재출전, 최대 2초 부활 보호(발사 시 해제). RPG·수류탄은 중앙 보급품을 주워 사용합니다.
- 공통: 전원 준비 → 누구나 현재 인원으로 시작 제안 → 전원 수락 → 3초 후 시작. 거절·20초 시간 초과·인원 변경은 요청을 취소합니다. 게임 도중 신규 참가를 받지 않습니다.
- 전투 중 Esc는 자신의 조작만 멈춥니다. 다른 사람의 게임은 계속됩니다. 호스트 탭을 뒤로 보내면 브라우저의 백그라운드 제한으로 게임이 느려지거나 연결이 종료될 수 있으므로, 방장은 경기 중 탭을 전면에 유지하세요.
- 기본 방장이 판정을 담당합니다. 호스트가 이탈하거나 연결이 끊기면 경기를 종료합니다. 호스트 자동 이관은 포함하지 않았습니다. 방 목록에 HOST와 직접 연결 핑이 표시됩니다.

## 비용과 연결 범위

현재 이미 사용하는 **Firestore**를 재사용해 새 DB 설정을 줄였습니다. 방/준비/투표/접속용 SDP만 저장하고, 대기 중 생존 확인은 참가자당 60초에 한 번입니다. 매 프레임 위치·탄환·피해는 DB에 쓰지 않습니다. 경기 시작 후 목록 구독과 DB 생존 확인을 중단하고, 경기 패킷은 WebRTC로만 전송합니다.

Firestore Spark의 기본 무료 한도는 일일 읽기 50,000회·쓰기 20,000회, 저장 1GiB입니다. **앞서 Realtime Database 기준으로 설명했던 동시 연결 100개 수치는 이 Firestore 방식에 적용되지 않습니다.** 대기 인원/방 구독이 많아지면 무료 읽기 한도에 먼저 도달할 수 있습니다. 현재 목록은 최근 방 최대 50개 범위의 대기 인원을 표시하며 전 세계 전체 사용자 통계가 아닙니다. 장시간 대기·다수 방의 상시 운영까지 무제한 무료를 보장하지 않습니다. 무료 한도 초과는 자동 과금이 아닌 서비스 제한으로 처리되도록 Spark를 유지해야 합니다.

정상 방 나가기 시 방장 방 문서와 해당 접속 문서를 정리합니다. 강제 종료된 대기실은 약 150초 후 목록에서 제외합니다. 비정상 종료로 남은 만료 문서는 Firestore 콘솔에서 주기적으로 삭제할 수 있습니다. 유료 TTL 삭제 기능은 사용하지 않습니다.

WebRTC는 공개 STUN으로 직접 연결합니다. CGNAT·회사망·일부 공유기 환경에서는 연결되지 않을 수 있고, 이 경우 유료 중계로 자동 전환하지 않습니다. 연결 대기 시간이 지나면 안내를 표시합니다. 일반 인터넷 회선/모바일 데이터 비용은 각 이용자의 기존 요금제에 따릅니다.

호스트가 전투를 판정하고 클라이언트는 이동을 예측합니다. 60Hz 이동 시뮬레이션, 20Hz 상태 전달, 입력 순번/중복 제거/속도 예산, 보간 및 최대 150ms PvP 되감기 판정을 사용합니다. 0ms 지연 또는 모든 환경의 연결 성공을 보장하지는 않습니다. 호스트 자체의 코드 변조까지 막는 경쟁전 보안 서버는 아닙니다. 기존 랭킹도 브라우저에서 점수를 계산하므로 규칙은 데이터 형식과 기존 기록 보호를 담당하며 점수의 진위를 검증하는 서버를 대신하지 않습니다.

## 변경 파일

| 파일 | 역할 |
|---|---|
| `firebase-network.js` | 기존 Firebase 앱/Firestore 연결 재사용, 멀티 익명 인증, 오류 안내 |
| `cloud-ranking.js` | 최신 랭킹 프로토콜을 유지하며 공용 연결 재사용 |
| `multiplayer-policy.js` | 정원·팀·준비·시작 동의 상태 전환 |
| `multiplayer.js` | Firestore 매칭/대기실, WebRTC 연결/패킷/연결 종료 |
| `multiplayer-combat.js` | 호스트 전투 판정, 협력전/PvP, 입력 예측·동기화 |
| `game.js` | 기존 전투와 멀티 연결, 사람형 모델, 독자적인 창고 맵 |
| `multiplayer-ui.js`, `multiplayer.css`, `index.html` | 매칭·팀·준비·투표 화면과 HUD |
| `firestore.rules` | 기존 랭킹 및 멀티 데이터 접근 규칙 |

## 검증

저장소 루트에서 별도 패키지 설치 없이 실행:

```sh
node tests/multiplayer.cjs
node tests/multiplayer-network.cjs
node tests/ranking-current.cjs
```

검사 내용: 4/8인 정원, 전원 준비/수락, 거절·만료, 같은 시드의 맵, 협력 HP/파츠 배수, 실제 게임 코드의 호스트→클라이언트 복제, 이동 예측/확인, 게스트 광역 피격, PvP 벽/아군 피해, 실드/탄약/30킬 종료, 연결 정보 교환과 큰 패킷 재조립, 방 종료 정리, 기존 랭킹 저장·정렬·구형 기록 호환. 네트워크 테스트는 프로세스 내부의 Firestore/WebRTC 모의 전송입니다.

실제 Firebase 규칙 에뮬레이터 및 서로 다른 회선의 브라우저를 이용한 접속·핑 검증은 이 환경에서 수행하지 못했습니다. 콘솔 설정 후 두 기기에서 협력전 2인 및 PvP 1대1을 먼저 확인하고, 이어 4/8인으로 확인해 주세요. 에뮬레이터가 필요한 테스트는 무료 로컬 Firebase Emulator Suite에서 진행할 수 있습니다.

공식 참고: [Firestore 무료 한도](https://firebase.google.com/docs/firestore/quotas), [Spark 요금제](https://firebase.google.com/docs/projects/billing/firebase-pricing-plans), [익명 인증](https://firebase.google.com/docs/auth/web/anonymous-auth), [WebRTC TURN](https://webrtc.org/getting-started/turn-server).
